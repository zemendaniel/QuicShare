apt install docker-buildx

docker buildx build -f Dockerfile -t quicshare:local .

xhost +local:docker  

docker run -it --rm \
    --network host \
    -e DISPLAY=$DISPLAY \
    -v /tmp/.X11-unix:/tmp/.X11-unix \
    -v /:/host \
    quicshare:local
